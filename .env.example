# Notion: create an internal integration, share the Production database with it.
NOTION_TOKEN=secret_notion_integration_token
NOTION_DATABASE_ID=your_notion_database_id

# After Notion sends the one-time verification POST, copy the token from Vercel logs
# and paste it here so incoming events can be checked (X-Notion-Signature).
# NOTION_WEBHOOK_VERIFICATION_TOKEN=secret_paste_from_verification_payload

# Telegram: create a bot with BotFather.
TELEGRAM_BOT_TOKEN=123456789:AA-your-bot-token-from-botfather

# Public URL of this Next.js app (no trailing slash), used by set-webhook helper.
NEXT_PUBLIC_APP_URL=https://anvance-bot-production.vercel.app

# Optional: 1–256 chars, only A–Z a–z 0–9 _ - (Telegram rule). NOT the bot token (contains ':').
# Example: output of openssl rand -hex 32
TELEGRAM_WEBHOOK_SECRET=abcdef0123456789abcdef0123456789

# One-time setup: protect /api/telegram/set-webhook?secret= and optional
# GET /api/notion/webhook?secret= (read last Notion verification_token; best-effort on Vercel).
ADMIN_SETUP_SECRET=long-random-admin-secret

# Map Telegram user id -> assignee display string as it appears in Notion (People names or plain text).
# Example: {"123456789":"Albert Rhey Embalsado"}
TELEGRAM_USER_ASSIGNEE_MAP={}

# Optional (recommended for teams): Notion database id for Telegram ↔ assignee rows.
# Users can self-register with /link (name or workspace email when Notion exposes it). Defaults:
#   "Telegram user id" (Number / Rich text / Title)  |  "Notion assignee" (Title / Rich text)
# NOTION_TEAM_LINK_DATABASE_ID=
# NOTION_TEAM_LINK_TELEGRAM_PROP=Telegram user id
# NOTION_TEAM_LINK_ASSIGNEE_PROP=Notion assignee

# Optional: Telegram user ids (JSON array) allowed to use /ops team|check|link…
# TELEGRAM_OPS_USER_IDS=[6160849706]

# If your Notion property labels differ from the CSV export, override them here.
# NOTION_PROP_NAME=Name
# NOTION_PROP_ASSIGNEE=Assignee
# NOTION_PROP_DUE=Due date
# NOTION_PROP_SHOOT=Shoot / live date
# NOTION_PROP_STATUS=Status
# NOTION_PROP_PRIORITY=Priority
# NOTION_PROP_DELIVERABLE=Deliverable
# NOTION_PROP_CLIENT=Client
# NOTION_PROP_SERVICE_LINE=Service line
# NOTION_PROP_REVIEWER=Reviewer
# Column on Production that holds the Telegram numeric id (source of truth for /mine).
# /link stamps this column; Notion webhooks and morning digest use it for DMs.
# NOTION_PROP_TELEGRAM_USER_ID=Telegram user id

# Column where the bot writes "Send to client" state (defaults below).
# NOTION_PROP_CLIENT_APPROVAL=Client approval
# NOTION_CLIENT_APPROVAL_SENT_VALUE=Sent
# NOTION_STATUS_CLIENT_REVIEW=Client review

# Optional: Telegram user ids (JSON array) notified when "Send to client" fires.
# TELEGRAM_SOCIAL_MANAGER_IDS=[6160849706]

# Optional: secret shared with Vercel Cron for the morning digest.
# Vercel auto-sets a Bearer token on cron invocations; if you use ?secret=...
# for manual runs, the ADMIN_SETUP_SECRET above is accepted too.
# CRON_SECRET=another-long-random-secret

# Morning digest schedule: runs at 06 and 07 UTC, but only sends when the local
# hour equals TELEGRAM_DIGEST_HOUR in TELEGRAM_DIGEST_TZ (defaults Europe/Rome 8).
# This auto-corrects across CET ↔ CEST (DST).
# TELEGRAM_DIGEST_TZ=Europe/Rome
# TELEGRAM_DIGEST_HOUR=8
